coverage:
  badge:
    path: .github/coverage.svg
  paths:
    - coverage.out
  acceptable: 80%
comment:
  if: is_pull_request
  deletePrevious: true
summary:
  if: true
report:
  if: is_default_branch
  path: .github/octocov.json
push:
  if: is_default_branch
  message: Update by octocov [skip ci]
